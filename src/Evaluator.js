

class Evaluator {
    /**
     * @constructor
     */
    constructor() {
        this._ast = {};
    }

    /**
     * Evaluates a given expression or program
     * @param {ASTNode} node 
     * @returns {void}
     */
    eval(node) {
        
    }
}