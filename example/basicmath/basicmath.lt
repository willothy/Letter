#dependency @../print.lt
#define pi 3.1415926

proc int main() {
    let int radius = 5;
    let float circumference = 2 * pi * radius;
    let double c = circumference;
    printDouble(c);
    return 0;
}
