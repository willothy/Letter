extern int printf(char* str);

proc void printInt(int v) {
    printf("%d\n", v);
}

proc void printDouble(double v) {
    printf("%f\n", v);
}

proc double printFloat(float f) {
    let double val = f;
    return val;
}

proc void print(string str) {
    printf(str);
}