#define pi 3.1415926
#dependency "example/print.lt"


proc float circumference(float radius) {
    return 2 * pi * radius;
}

proc int main() {
    let double value = circumference(5.0);
    printDouble(value);
    return 0;
}