class Point {
    proc constructor(x, y) {
        this.x = x;
        this.y = y;
    }

    class Test {
        proc constructor(str) {
            this.expect = str;
        }

        proc calc() return this.x + this.y;
    }
}

let point = new Point(x, y);